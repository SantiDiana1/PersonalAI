# Copia este archivo a .env y ajusta los valores. Nunca commitees .env real.

# Postgres local (docker-compose.dev.yml)
POSTGRES_USER=personalai
POSTGRES_PASSWORD=personalai
POSTGRES_DB=personalai
POSTGRES_PORT=5432
