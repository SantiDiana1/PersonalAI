{
  "name": "@personalai/control-bot",
  "version": "0.0.0",
  "private": true,
  "description": "Bot de control de Telegram: comandos deterministas sin modelo ni tokens de cuota",
  "type": "module",
  "main": "./dist/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@personalai/shared": "workspace:*",
    "pg": "^8.13.1",
    "pino": "^10.3.1"
  },
  "devDependencies": {
    "@types/pg": "^8.11.10",
    "typescript": "^5.7.2",
    "vitest": "^2.1.5"
  }
}
