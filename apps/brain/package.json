{
  "name": "@personalai/brain",
  "version": "0.0.0",
  "private": true,
  "description": "Personal Brain — servicio de memoria (ingestion/consolidation/retrieval/api)",
  "type": "module",
  "main": "./dist/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@personalai/shared": "workspace:*",
    "pg": "^8.13.1",
    "pino": "^10.3.1",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@types/pg": "^8.11.10",
    "typescript": "^5.7.2",
    "vitest": "^2.1.5"
  }
}
