{
  "name": "@personalai/brain-mcp",
  "version": "0.0.0",
  "private": true,
  "description": "Servidor MCP adaptador — expone la API de Brain como tools MCP",
  "type": "module",
  "main": "./dist/index.js",
  "bin": {
    "brain-mcp": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "@personalai/shared": "workspace:*",
    "pino": "^10.3.1",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "typescript": "^5.7.2",
    "vitest": "^2.1.5"
  }
}
